import { Text, View } from 'react-native';

const Signin = () => {
  return (
    <View className="flex-1 justify-center items-center bg-cyan-200">
      <Text className="text-black dark:text-red-600 font-bold text-hero">
        SignUp
      </Text>
    </View>
  );
};

export default Signin;
